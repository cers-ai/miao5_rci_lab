# 妙悟·实时认知智能实验室 V0.1 测试报告

测试时间：2026-09-09  
测试环境：Windows、本机 CPU、Python 3.11.12、Node.js 22、ffmpeg 8.1.1。  
结论：V0.1 自动化与真实模型链路通过；前端运行于 `http://localhost:3000`。pyannote 与真实大模型综合分析分别等待用户提供官方模型授权和模型服务配置，系统对此显示真实失败/待配置状态。

## 构建与运行

| 检查 | 结果 |
|---|---|
| Python 源码编译 | 通过 |
| React TypeScript + Vite 生产构建 | 通过，1583 modules transformed |
| 后端健康检查 | 200 OK |
| 前端 3000 端口 | 200 OK |
| 3000 → 后端同源代理健康检查 | 200 OK |
| 桌面布局 1440×1000 | 截图核验通过 |
| 小屏布局 390×844 | 无横向溢出，截图核验通过 |

## 模型下载与真实加载

| 模型 | 固定版本 | 本地状态 | 真实测试结果 |
|---|---|---|---|
| CAM++ 中文 | v2.0.2 | 已下载、SHA256 清单已保存 | 可用；192维 L2 归一化声纹 |
| FunASR FSMN VAD | v2.0.4 | 已下载、SHA256 清单已保存 | 可用；测试语音检测到 0～3700ms |
| WeSpeaker 中文 ResNet34-LM | official-oss-1714056968 | 已下载、SHA256 清单已保存 | 可用；256维 L2 归一化声纹 |
| Paraformer Streaming | v2.0.4 | 已下载、SHA256 清单已保存 | 可用；分块流式调用返回中文文本 |
| pyannote Community-1 | 授权后固定commit | 未安装 | 按用户选择待配置；UI/接口显示需要接受协议并设置HF_TOKEN |

模型目录启动加载前会重新核验清单 SHA256，文件变化会拒绝加载。当前四个公开模型均使用真人中文测试语音，不使用正弦波或随机结果。

## 真人语音声纹与实验结果

测试音频来自 CAM++ 官方模型仓库的三份中文真人语音。相同说话人两段和另一说话人一段用于基础鉴别：

| 模型 | 相同人物余弦相似度 | 不同人物余弦相似度 |
|---|---:|---:|
| CAM++ | 0.694 | -0.084 |
| WeSpeaker | 0.643 | 0.028 |

相同人物得分在两个模型上均显著高于不同人物。该结果验证模型加载、特征前端和相似度方向，不宣称通用识别准确率。

完整链路把三份真人语音按 `人物1 → 1秒静音 → 人物2 → 1秒静音 → 人物1` 组合为受控双人测试：

- E1 CAM++ 固定2人：发现2位，耗时约1.13秒。
- E1 CAM++ 自动人数/距离阈值：发现2位，耗时约0.82秒。
- E1 WeSpeaker 固定2人：发现2位，耗时约0.50秒。
- E2 使用 WeSpeaker 建档：同一人物录音最高相似度0.780，另一人物0.179；阈值0.6时分别识别为目标和非目标。
- E3 按真实时间回放约5秒，首次锁定约1.60秒。保存 Ground Truth 后误锁0次、目标覆盖率约67.3%；最后一个目标持续到录音结束，因此解锁时延为空而非伪造数值。
- 不同模型空间强制混用会生成失败 Run 并保存“模型必须与建档模型一致”，不会继续计算。
- 浏览器 WebSocket 使用真实 PCM16 数据完成 ready、逐窗口推理、人工事件、stop、录音保存和完成状态。

这些组合音频只验证端到端执行及持久化，不替代真实会议、多设备、噪声和重叠讲话条件的实验质量验收。

## 自动测试

全量后端测试：`18 passed`。

- 密码PBKDF2加盐、登录限流、Session、改密失效、管理员/实验员权限。
- 跨站写入阻止、服务API Key加密与脱敏、密钥保留和清除语义。
- 非法/空音频拒绝、真实ffprobe/ffmpeg转码、Range音频读取、文件引用删除保护。
- 模型缺失时真实失败、状态持久化、模型适配器与命令行协议校验。
- 锁定/解锁迟滞、无语音解锁、标注合并、漏锁、无目标、覆盖率和时延口径。
- 人工核验对象校验、审核人持久化、运行完成约束。
- 隔离 OpenAI Compatible HTTP 服务验证 `/models`、`/chat/completions`、结构化JSON校验、上下文过滤。
- MD/CSV/JSON 报告一致性、分析后核验变化阻止旧分析生成新报告、既有报告快照不可变。
- CAM++/WeSpeaker/VAD真实推理、E1三轮、E2、E3回放和WebSocket全链路。

浏览器端测试：`3 passed`。

- 登录、首页、设置、A02占位、真人录音上传、E1运行、片段试听、人工核验、E2建档与识别、E3回放、报告下载与数据仓重下载。
- 390×844小屏布局和退出登录。
- 浏览器同源WebSocket发送真人PCM、真实模型处理、实时实验保存和页面记录展示。

自动化没有替代真实硬件点击授权的主观验证；麦克风前端增加了权限、AudioWorklet和WebSocket连接超时及明确错误。后端真实WebSocket和浏览器PCM传输已经覆盖。

## 已知边界

1. pyannote Community-1 是官方受限模型。用户已选择先完成公开模型；授权前对应E1路线不可运行，并准确显示原因。
2. 综合分析需要用户自己的 OpenAI Compatible 服务。当前已验证真实HTTP协议、超时、错误和结构化响应处理；未配置时可生成明确标注“尚未智能分析”的证据报告。
3. 本机未检测到NVIDIA运行工具，所有性能结果为CPU路径。GPU需要在有CUDA环境中另行锁定PyTorch构建并复测。
4. 真正的多人会议识别质量需要用户会议音频和人工核验，不能由少量公开样本代替。

## 证据位置

- JUnit全量结果：`workspace/logs/tests.xml`
- 真实模型结果：`workspace/logs/real-model-tests.xml`
- 浏览器结果：`workspace/logs/browser-tests.json`
- 声纹比对：`workspace/logs/real-embedding-test.json`
- 端到端证据：`workspace/logs/real-pipeline-test.json`
- 界面截图：`workspace/logs/home-desktop.png`、`home-mobile.png`、`settings-desktop.png`、`e1-desktop.png`、`e3-desktop.png`、`microphone-desktop.png`
