# 妙悟·实时认知智能实验室 V0.1 开发计划

依据：`妙悟_实时认知智能实验室_V0.1.html`、`妙悟_实时认知智能实验室_V0.1_开发方案.md`。

## 范围与验收原则

- 完整实现登录、用户管理、文件仓、模型注册、模型服务、分析助手、A01-E1/E2/E3、综合分析和三种报告导出。
- A02、A03、A05、B01～B04 按开发方案保留导航与实验目的占位。
- 原型布局、中文术语、蓝白配色作为界面依据；不移植原型的模拟输出。
- 每次实验独立保存输入哈希、模型版本、参数、原始结果、耗时、核验和指标。失败必须保存原因，不产生伪造结果。
- 前端固定 http://localhost:3000（strictPort），后端 http://127.0.0.1:8000，经 Vite 同源代理访问 HTTP/WebSocket。

## 实施顺序

| 阶段 | 工作 | 验收证据 | 状态 |
|---|---|---|---|
| 1 | Python 3.11 环境、锁文件、React/TS/Vite、FastAPI/SQLite、初始化与启动脚本 | 构建与健康检查 | 进行中 |
| 2 | 会话登录、哈希密码、改密、管理员用户管理、音频上传/转码/试听/删除保护 | API 集成测试 | 待完成 |
| 3 | 模型下载、版本及校验和清单、真实加载测试、编辑和启停、服务密钥保护和真实请求测试 | 模型测试记录 | 待完成 |
| 4 | E1：FunASR VAD+CAM++聚类、pyannote、WeSpeaker聚类；分段试听、人工核验、多轮记录 | 真实语音推理与参数测试 | 待完成 |
| 5 | E2：选择/合并 Speaker、排除片段、声纹建档、跨文件验证、人工核验 | 建档与验证链路测试 | 待完成 |
| 6 | E3：AudioWorklet PCM/WebSocket、历史录音按真实时钟回放、同一状态机、事件/时间线、Ground Truth指标 | 状态机、WebSocket和回放测试 | 待完成 |
| 7 | 所选 Run 结构化上下文、真实大模型分析、持久化报告、MD/CSV/JSON下载 | 服务协议测试、导出一致性测试 | 待完成 |
| 8 | 全流程回归、浏览器检查、错误路径、操作说明、测试报告 | 测试报告.md | 待完成 |

## 模型准备

1. CAM++ 中文：iic/speech_campplus_sv_zh-cn_16k-common。
2. FunASR FSMN VAD：与 CAM++ 和 WeSpeaker 分段链路配套下载。
3. WeSpeaker：使用官方中文预训练 checkpoint；原型的 `wespeaker-ecapa` 仅为未锁定占位标识，界面必须显示实际架构与版本。
4. pyannote Speaker Diarization：从官方仓库下载。需要模型协议/token时记录“未安装/需要授权”，提供初始化重试入口，不冒充安装成功。
5. Paraformer Streaming：下载原型预设流式模型并实现加载测试；A02实验页面仍不在本期范围。

所有公开模型固定下载版本并生成文件 SHA256 清单；模型真实推理通过后才标为可用。初始环境未检测到 NVIDIA 工具，以 CPU 路径优先验证。

## 测试策略

- 单元：参数边界、聚类、声纹空间一致性、锁定状态机、人工标注指标、密码和数据验证。
- 集成：鉴权、用户权限、非法上传、音频转换、删除引用保护、失败记录、核验持久化、报告导出及密钥脱敏。
- 真实模型：公开真人语音样本，真实 VAD/embedding/E1/E2/E3 推理；测试样本来源与实验质量结论分开记录。
- 浏览器：登录、导航、上传、运行状态、结果详情、设置、报告；前端 TypeScript检查和生产构建。
- 外部服务协议可用隔离测试服务验证；只有真实配置的 LLM 返回才计作智能综合分析验收。未提供服务配置和授权不得记为已通过。
- 麦克风硬件权限与真人多人录音的主观准确率验收需要现场实验；自动测试结果不替代人工结论。

## 交付清单

源码、锁文件、初始化/下载/启动/健康检查脚本、本地模型及清单、README.md、开发计划.md、测试报告.md。
