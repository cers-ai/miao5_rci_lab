# A01 改造开发计划 v2（已批准，分阶段实施）

编制日期：2026-09-21  
需求依据：根目录《妙悟_实时认知智能实验室_A01改造方案-v2.md》全部 38 节。  
核对依据：当前后端、前端、测试代码及原开发计划、测试报告。  
状态：用户已批准并要求开始开发。阶段 0 的契约、隔离测试基线、备份恢复与迁移演练已完成，待阶段验收；阶段 1～8 尚未执行。正式业务代码、数据库及模型配置尚未改造。新结果见 `docs/a01-v2/阶段0-交付与验收报告.md`，原测试报告的“通过”不代表本次改造已验证。

## 一、分析结论与改造边界

本次改造的核心是建立一个完整、可追溯、可比较的离线目标人物识别实验，而不只是合并页面。实验必须能够回答：在明确的数据条件下，哪条 Discovery 路线、哪个目标模型、多少建档语音和什么阈值更合适，其误认风险与失败边界是什么。

最终 A01 仅有两个一级卡片：

1. **目标人物声纹识别实验室**：输入、Discovery、人工确认、建档、跨录音识别、评价、候选比较、保存 Run。
2. **分析与报告**：选择一个或多个 Run、真实智能分析、技术基线、综合报告与数据导出。

范围包含方案中的文件级与时间段级 Ground Truth、代表试听、15/30/60 秒建档、四类变量比较、阈值复用、错误样本与分布图，以及 Worker 隔离。

实时识别、麦克风、Lock/Unlock 和实时延迟退出 A01 的新实验流程。保留已有实时实验与报告的历史查阅能力，不在本期开发新的实时实验页面。A02/A03/B01 等只遵循未来设计原则，本期不改造其业务。系统设置、数据仓等仅做 A01 所需的关联调整。

不引入通用 Grid Search、Celery/Kafka 或新的云端依赖。优先沿用现有 FastAPI、SQLite、React、TypeScript、模型适配器与音频处理工具。

## 二、现状核对：可以复用什么、必须改什么

| 领域 | 当前代码事实 | 改造判断 |
|---|---|---|
| 运行隔离 | `backend/app/experiments.py` 使用单线程池；推理与 FastAPI 同一进程 | 线程无法隔离进程级崩溃，必须加入独立 Worker 与主进程监督 |
| 其它推理入口 | 启动模型测试、设置页模型测试、建档均可能在主进程执行；下载后测试也需检查 | 不能仅隔离 pyannote 的 Discovery 按钮，需覆盖相关加载与测试入口 |
| Discovery | 已有 pyannote 和 VAD＋CAM++/WeSpeaker 聚类分支 | 复用真实算法；当前可用性须重新验证，不能依据旧报告直接认定 |
| Speaker 试听 | 展示原始短片段；聚类路线使用约 1.5 秒窗口 | 增加代表试听生成与来源映射，不改变原始证据 |
| 目标建档 | 支持多 Speaker、排除片段、非重叠选择；按原顺序最多取 60 秒 | 增加确定性的质量筛选、15/30/60 秒变量、精确裁剪与时长不足处理 |
| 识别模型筛选 | 前端按 adapter 筛选；CAM++ 预设能力登记为“说话人发现” | 统一注册能力、可用状态与后端校验；支持模型具有多种能力 |
| WeSpeaker 名称 | 实际适配器为中文 ResNet34-LM ONNX | 如实显示实际架构与版本，不将方案示例 ECAPA 当作已安装模型 |
| 离线识别 | E2 已计算相似度、阈值判断、文件是否检出 | 复用推理，拆开得分计算与阈值评价，增加 Ground Truth 指标 |
| 标注与指标 | 当前正式 Ground Truth 接口只处理 E3 的时间线指标 | 新增 A01 离线评价模块，不套用实时锁定口径 |
| Run 与存储 | E1/E2/E3 分别是 Run；SQLite 表以 JSON 为主 | 增加完整 A01 Run、内部 Job、Candidate、版本化标注和证据关系 |
| 智能分析与报告 | 已有真实服务调用、结构化校验、报告快照；内容围绕 E1/E2/E3 | 复用调用与快照能力，重写离线证据结构、推荐约束及报告结构 |
| 自动测试 | 已有后端与 Playwright 测试；真实模型测试可因模型缺失而跳过 | 扩充关键算法与崩溃验证；将跳过、外部依赖未就绪与通过分开报告 |

旧《测试报告.md》记录的是 2026-09-09 的环境与结果，其中 pyannote 和真实 LLM 尚未完整验收；它不能解释本次用户反馈的崩溃原因。崩溃根因须在阶段 1 采集真实日志后判断。

## 三、实施前需要冻结的规则

以下是为消除方案歧义提出的实施约定；批准本计划即按这些约定推进。若需调整，在阶段 0 验收时冻结修订版本。

### 3.1 完整 Run、内部步骤和人工确认

- 一个 Run 对应同一目标人物、建档录音、测试集及其 Ground Truth 快照，可以包含多个候选组合。Discovery、建档、识别是内部 Job，不再独立冒充完整实验。
- 进入实验并首次执行 Discovery 时创建草稿 Run，所有中间证据自动持久化。人工确认阶段显示“等待目标确认”，不计为失败或完成。
- “开始实验”提交已确认的组合计划；“保存本次 Experiment Run”确认归档。计算结果自动保存，刷新或忘点保存不会丢数据。
- 多条 Discovery 路线分别产生自己的 Speaker ID。必须分别试听、确认目标 Speaker 及排除片段，不能假设不同路线的 Speaker 02 是同一个人。确认全部完成后，后续模型、时长、阈值组合才可自动执行。
- 修改建档文件或 Discovery 参数须重新确认；修改模型或建档时长须重建对应 Profile；只改变阈值不得触发模型推理。
- Worker 崩溃、超时或某个必需候选执行失败时，整体 Run 标记 `FAILED`，保存已完成候选及错误，不能显示“全部成功”。后续未执行候选注明原因；重试创建关联的新尝试，不覆盖失败证据。
- “运行成功”和“证据足以评价”分开：真实计算完成但标注不足可完成运行，评价资格与空指标必须清楚显示。

### 3.2 代表试听与建档取样

- 保留原始 Segment，另存试听片段清单、拼接偏移、源文件时间范围、排除原因和生成规则版本。
- 优先排除已知重叠、人工排除及明显异常片段；用可解释规则筛选较长、较少静音、较少削波的素材。不把聚类置信度包装为通用音质分数。
- 约 1.5 秒短窗先按同 Speaker、时间相邻且无其它 Speaker 插入等规则形成连续候选块；不得凭标签相同跨越其它人的讲话进行源音频长段截取。
- 优先选 3～5 个代表块，目标约 15～30 秒，块间加入短静音；必要时使用更多短块补足试听。素材不足时提供真实可用长度并标明原因，不重复播放同一段凑时长。
- 聚类路线若无法可靠检测重叠，显示“重叠检测能力有限”，不能声称全部重叠已清除；保留人工排除入口。
- 建档时长只计算目标人物有效源音频，不包含试听插入的静音；目标 15/30/60 秒按采样点裁剪。素材不足的时长候选标记不可运行，不把 42 秒标成 60 秒。
- 不同建档时长采用同一确定性素材优先顺序，尽量形成嵌套选段。保存实际用段、实际时长、聚合规则、模型权重标识和向量维数，保证可复现。

### 3.3 Ground Truth 和评价口径

**文件级（必做）**

- 使用稳定的目标人物 ID 关联标注，姓名作为显示属性。文件标注区分“未标注／有／无”，不将空白视为无目标。
- 正式开始评价实验前，每个测试文件必须完成文件级标注。无标注文件不能偷偷进入分母。
- 默认文件判定：至少一个有效识别片段相似度达到阈值，则文件判为有目标。该规则及切窗配置写入 Run，后续若改变须生成新版本。
- 根据文件混淆矩阵计算 Precision、Recall、F1、False Accept Rate、Miss Rate；明确标注“文件级”、正负文件数、TP/FP/FN/TN。
- 文件级 False Accept Rate = FP / (FP + TN)，Miss Rate = FN / (TP + FN)。不能用 FP / (TP + FP) 冒充 False Accept Rate。

**时间段级（用于正式定位评价）**

- V0.1 支持选择少量代表文件，但选定文件须完整标注目标人物的全部出现区间，并由用户确认“该文件目标时段已标完整”。只标几个示例片段不能将其它时段全部推断为非目标。
- 文件标注为“无”时可形成空目标区间；“有”但未完成时间段标注时，仅参加文件级评价。区间越界、负时长和文件级矛盾必须阻止保存。
- 默认采用时间长度加权指标，评价范围为完整标注文件的完整时间轴。先合并重叠区间，避免重复累计；以半开区间和统一毫秒精度处理边界，默认不增加容忍区间。
- 令 P 为预测目标区间并集，G 为目标标注并集，T 为评价时间轴：TP = |P∩G|，FP = |P−G|，FN = |G−P|，TN = |T−(P∪G)|。
- Precision = TP/(TP+FP)，Recall = TP/(TP+FN)，F1 = 2TP/(2TP+FP+FN)，False Accept Rate = FP/(FP+TN)，Miss Rate = FN/(TP+FN)。跨文件主汇总先累加时长再计算，同时保留逐文件结果。
- 此口径的 False Accept 表示“非目标时间被判为目标”的比例，分母包括静音，因此必须显示误认总秒数、评价总时长和分母说明。若要单独报告“其它人讲话中的误认率”，须额外具备可信的语音区域标注，本期不能凭目标标注假装具备。
- 分母为零时显示 N/A 及原因。无预测但存在真实目标时 Recall 和 F1 为 0；无正样本不能宣称召回率为 100%。
- VAD 漏掉的目标时段仍计入 FN，错误样本允许无相似度并说明“未生成有效识别片段”。仅评价识别器输出的片段会漏掉这类错误，必须避免。
- 文件级和时间段级指标分别展示、排序、导出，禁止混用。正式时间段指标仅反映已完整标注的子集，必须显示覆盖范围。

### 3.4 比较公平性与结论资格

- 建档与测试音频不得使用相同文件或相同内容哈希；也提示避免同一会议切段同时用于建档和测试，因为文件哈希不能识别此类泄漏。
- 候选比较固定目标、测试文件集合、标注版本、评价口径及识别切窗规则。跨 Run 不满足这些条件时可以并列查看，但必须分组，不自动形成统一胜负排名。
- 阈值默认扫描 0.60～0.90、步长 0.02，共 16 个阈值；保留合法范围 [-1,1] 的显式设置，模型得分是余弦相似度，不是概率。
- 例：2 条路线 × 2 个模型 × 3 种时长 = 12 个推理配置，乘以 16 个阈值产生 192 个评价候选，不能当作 192 次模型推理。
- 默认同口径按 F1 降序排序，同分优先较低 False Accept，再优先较短建档时长；支持 Precision、Recall 和 False Accept 排序。推荐规则与版本保存到证据中。
- 稳定区间默认使用与推荐阈值连续相邻的已测点：F1 相对推荐点下降不超过 1 个百分点且 False Accept 上升不超过 0.5 个百分点。此为待批准的工程规则，不是统计置信区间；不足 3 个可比点不宣称稳定。
- 未设置业务误认上限时，只推荐“当前数据上的候选配置”，不自动宣布已满足业务风险要求。最终技术基线由用户确认，并记录容忍上限及证据。
- 调参数据上的最优成绩标明“调参集结果”。正式基线建议另建使用新会议录音、固定配置且不再次扫描阈值的验证 Run；没有独立验证时明确标注“待验证”。不新增通用数据集管理平台。

### 3.5 数据兼容、缓存和报告

- 沿用 SQLite，增加带版本的数据结构、必要查询索引和迁移脚本；保留历史 JSON 数据可读。新增 `model_jobs`、`ground_truth_files`、`ground_truth_segments`、`experiment_candidates`、识别得分证据，并扩展 Run/Profile。
- Run 保存输入哈希、模型及权重版本、实际设备、参数、代码版本或源码指纹、标注快照、人工确认、缓存来源、时间及错误。Profile 保存选段出处与模型空间信息。
- 模型得分与阈值判定分层保存，避免每个阈值复制完整向量和音频。不同 Candidate 引用不可变得分集合，导出时可展开对应判断。
- 缓存按音频内容、切段/预处理参数、模型权重、选段、聚合规则等真实依赖失效。测试音频 embedding 可跨 Profile 复用；相似度按 Profile 计算一次；阈值只做判断和评价。
- Ground Truth 修改生成新版本，已有 Run 和报告保持原快照；“按新标注重新评价”生成关联版本并复用原始得分，不能静默改写历史结论。
- 历史 E1/E2/E3 记录作为“旧版实验步骤”保留，不能自动拼成未经验证的新完整 Run；不加入新 A01 排名。
- 新 A01 的下载入口集中于“分析与报告”；数据仓新记录导向该卡片，旧版报告保留历史查阅与下载。
- 综合报告默认沿用 Markdown，另有便于比较的候选级 CSV 与完整证据 JSON。Word/PDF 不在本期新增范围。

## 四、分阶段开发与验收

执行顺序为阶段 0→8。每阶段提交“变更、测试命令与结果、真实运行证据、已知限制、用户验收步骤”。阶段验收通过后进入下一阶段；如用户希望合并验收，可明确授权合并，但测试门槛不降低。

### 阶段 0：冻结契约、数据迁移方案与验证基线

**开发目标**：在重构前明确业务口径、数据关系、历史兼容与可执行的验收样本。

**开发策略和规范**：

- 冻结第三节规则、状态流转、API 请求/响应及错误格式，建立需求编号到阶段、测试和证据的映射。
- 盘点实际环境、模型可用状态、真实 LLM 配置是否就绪及代表录音；只报告就绪状态，不输出密钥。
- 固定当前代码基线，设计一致性数据库备份与 workspace 证据备份，包含恢复所需密钥文件但不提交到代码仓库。运行中 SQLite 用一致性备份方式，不只复制主数据库文件。
- 制定可重复迁移、版本检查、历史记录读取与回滚方案；此阶段先在副本验证，不改正式数据。

**我的测试与验证要求**：在隔离 workspace 记录现有测试及构建基线；所有失败与跳过分类。验证备份可恢复、历史 Run/Profile/报告数量与引用可核对。

**需要你验收的点**：两卡片流程；指标口径；完整标注语义；跨路线人工确认；误认上限是否已有业务要求；指定验收录音与目标人物。无业务上限时采用“候选基线待确认”。

**交付与通过门槛**：契约、数据字典、迁移/恢复方案、基线报告和验收样本清单；上述规则冻结后进入阶段 1。

### 阶段 1：模型 Worker 隔离与失败可观测性（最高优先级）

**开发目标**：pyannote 或其它 A01 模型进程崩溃时，FastAPI 仍可使用，实验能准确结束并保存错误。

**开发策略和规范**：

- 采用适配 Windows 的独立 subprocess Worker，主服务只做任务调度、监督与结果落库；默认一次执行一个模型重任务。按任务使用独立进程，任务内允许复用模型。
- 覆盖 Discovery、建档、识别、启动自检、设置页测试和下载后的测试；A01 新流程及设置入口不在主服务中执行 Torch/模型加载推理。
- Job 保存 job_id、run_id、操作、模型、PID、起止时间、状态、退出码、错误及内存观测；记录峰值指标的单位和测量方式。无法采集时写未知及原因，不能补零。
- 区分正常退出、Python 异常、进程异常退出、超时、用户取消与服务重启。监督逻辑检查退出码和结果完整性，不能只等待返回文件。
- 设置任务超时、心跳、取消和子进程清理；防止 stdout/stderr 堵塞；限定内部协议和产物目录。正式进程隐藏启动。
- 模型并发与资源预算留出主服务余量；在本机验证可用的进程内存限制机制。进程隔离本身不能保证整机内存耗尽时系统仍正常，因此不以“已分进程”替代资源测试。

**我的测试与验证要求**：真实可用模型成功执行；独立测试进程非零退出、无输出、超时、取消、受控内存限制触发及服务重启；每类故障期间检查健康接口、登录和读取历史记录仍成功，随后新任务可运行，无遗留进程。故障注入不向实验伪造推理结果。pyannote 授权可用时必须单独完成真实推理及隔离验证。

**需要你验收的点**：一次真实 Discovery；一次受控 Worker 故障后页面仍可使用、错误可读、可重新运行。

**交付与通过门槛**：Worker、任务日志、故障测试记录、资源边界说明。若缺少 pyannote 授权，隔离机制可单独验收，但 pyannote 路线实测仍保留待验收，不标记通过。

### 阶段 2：代表试听、目标确认与两卡片页面骨架

**开发目标**：用户不必逐个听碎片即可辨认 Speaker；Discovery 与目标确认进入同一实验室。

**开发策略和规范**：

- 按第三节规则生成真实试听音频，显示 Speaker、有效总时长、片段数、试听与材料不足提示；原始片段在详情中可查。
- 支持多 Speaker 合并为目标、排除错误片段；保存确认人、时间和版本，切换路线不会错误沿用选择。
- 将 A01 顶层改为两卡片，实验室按需求建议顺序安排内部区域；尚未完成的操作明确不可用。
- 按实验室、标注、结果、分析报告等职责拆分前端，新增契约使用明确 TypeScript 类型，不继续堆叠单文件 `any` 状态。

**我的测试与验证要求**：验证试听实际采样与来源区间一致、插入静音长度、重复段去除、重叠排除、短素材退化、无可用片段；浏览器验证播放、选择合并、排除、刷新恢复和不同路线选择隔离。

**需要你验收的点**：使用实际多人会议录音试听，各 Speaker 能否较容易辨认；错误片段是否容易排除；目标确认是否清楚；两卡片结构是否符合预期。

**交付与通过门槛**：可试听的真实产物及映射清单、页面截图、浏览器验证记录；材料不足如实显示。主观试听验收不承诺算法永远能正确分人。

### 阶段 3：两级 Ground Truth 与可复用标注

**开发目标**：每个测试文件有明确目标有/无标注，并能给少量代表文件完整标注目标时间段。

**开发策略和规范**：

- 实现文件级标注、时间区间编辑、区间试听、删除/修改及“已标完整”确认；支持易读时间格式输入，内部统一毫秒。
- 稳定关联目标人物与文件，标注保存版本、作者、更新时间，Run 冻结快照。
- 处理重叠标注合并、越界、标注矛盾、未标注与空目标的区别；测试集去重并禁止建档同源哈希。
- 不扩展成全功能多人转写或标注平台。

**我的测试与验证要求**：正负文件、未标注、空目标、区间乱序/重叠/接触/越界、修改姓名、标注版本变更与旧快照保持；刷新后标注可恢复。

**需要你验收的点**：完成一个含目标文件、一个无目标文件的文件标注；给一个代表文件标完整目标时间段并逐段试听；确认界面不会把未标注当成无目标。

**交付与通过门槛**：两级标注 API/界面、版本记录、校验测试；标注数据可以支撑后续真实指标核对。

### 阶段 4：可控时长 Profile 与单组合完整实验

**开发目标**：完成从建档录音到跨录音识别的一个完整 A01 Run。

**开发策略和规范**：

- 支持已确认目标的 15/30/60 秒确定性选段，Worker 内真实建档；记录实际模型、向量与来源关系。
- 注册能力支持多值并兼容旧值；模型列表只显示已启用、可用且包含“目标说话人识别”能力的模型，后端同样验证。
- Discovery 路线与目标识别模型解耦；如 CAM++ 发现＋WeSpeaker 建档是合法组合，但识别必须使用与 Profile 一致的模型空间。
- 将得分生成、阈值判断、文件判断拆分；先打通单组合，再做批量。Run 从草稿到等待确认、排队、运行、完成/失败/取消可追溯。
- 刷新恢复、重复提交防重、输入快照、自动保存与归档交互同时实现。

**我的测试与验证要求**：真实模型跨文件推理；15/30/60 秒取样误差控制在音频采样精度；不足时长、重叠/排除、模型停用或版本改变、无语音、无目标、文件损坏、跨模型空间拒绝、重复点击、刷新与失败保存。

**需要你验收的点**：完成一个真实完整 Run；查看 Profile 实际时长和来源；确认测试文件中的预测与试听一致；刷新后继续查看结果。

**交付与通过门槛**：单组合完整流程、Profile/Run 证据、真实模型验证结果；此阶段未接入的正式指标明确显示待评价，不能展示占位成绩。

### 阶段 5：正式指标、排名基础与错误定位

**开发目标**：从真实预测和 Ground Truth 得到可独立核算的五项指标，并定位误认与漏识别。

**开发策略和规范**：

- 使用独立纯计算模块实现第三节固定公式；前后端与导出复用相同计算结果，统一指标版本。
- 展示文件级与时间段级的口径、覆盖范围、分母和 N/A 原因；False Accept 显著展示。
- 生成误认区间 P−G、漏识别区间 G−P，并映射音频试听；跨越标注边界的预测片段可拆分错误子区间。
- 只有文件级标注时提供文件级错误；正文件的预测片段不能全部标成真实目标，也不能假造漏识别时间。

**我的测试与验证要求**：用可手算区间做算法测试，覆盖完全正确、全错、全正、全负、无预测、无正样本、零分母、重叠与边界、部分标注文件、VAD 漏检；真实会议样本抽查并独立重新计算混淆时长。数学测试固定输入只验证公式，不作为产品实验成绩。

**需要你验收的点**：挑选实际错误，试听后核对误认/漏识别分类；对照一小段人工时间轴确认公式；确认两种指标含义及 False Accept 分母易理解。

**交付与通过门槛**：指标说明、可手算对照表、真实实验指标证据、错误试听；没有 Ground Truth 的项目无虚构准确率。

### 阶段 6：A01 自动组合比较、阈值复用与诊断

**开发目标**：在人工确认各路线后自动比较四类变量，给出可解释排名、稳定区间和诊断图。

**开发策略和规范**：

- 提交前显示路线、模型、时长、阈值数量、待执行组合和不可运行原因；设置可配置组合上限，避免误触发大量任务。
- 建立 Discovery→Profile→测试 embedding→相似度→阈值评价的依赖执行关系。只对真实依赖变化重新计算，并记录命中来源。
- 按第三节规则实现排序与稳定区间；临界样本默认 |score−threshold|≤0.02，注明这是审查范围，不是真实错误标签。
- 相似度图仅使用 Ground Truth 可确定标签的片段；混合目标/非目标的窗口单列“混合/不确定”。只有文件标签时不伪造 Target/Non-target 片段分布。
- 显示得分缺失、样本数量和标注覆盖，不能通过图形掩盖漏检；支持错误、临界样本筛选试听。

**我的测试与验证要求**：核对组合去重和数量；实际记录模型调用计数，1 个阈值扩展为 16 个阈值不得增加模型推理次数；扫描结果与对同一得分逐阈值独立计算一致；模型/音频/选段变化准确失效；稳定/敏感/端点/缺失点测试；中途失败与取消保留证据。记录本机实际耗时及峰值内存。

**需要你验收的点**：运行至少 2 条已通过真实测试的路线 × 2 个可用目标模型 × 可满足的时长；查看排名、切换排序、试听误认与临界样本；调整阈值确认无需重新跑模型。

**交付与通过门槛**：自动对比 Run、调用计数与缓存证据、排名/曲线/分布、稳定区间说明。素材足够时完整验收 15/30/60 秒；不足的项保留待验收。

### 阶段 7：分析与报告、真实智能分析与技术基线

**开发目标**：基于一个或多个可追溯 Run 回答方案要求的九个问题，并下载一致的报告。

**开发策略和规范**：

- 历史 Run 选择、摘要、可比性分组、真实助手调用、结果展示、基线确认、下载集中在第二卡片。
- 复用系统“辅助分析智能助手”配置；更新 A01 专用分析契约。对用户自定义提示词保留原值，追加 A01 必须遵守的证据约束，不静默覆盖配置。
- 发送真实指标、候选、错误摘要、稳定区间与人工核验；不发送音频、声纹向量、密钥或无关本地路径。上下文过大时按确定规则摘要并记录省略量。
- 明确覆盖：路线、模型、时长、阈值、稳定区间、误认原因、失败边界、基线资格、下次实验。只有一个路线或模型时明确“未比较”，不能凭常识编造胜负。
- 推荐值必须引用实际 candidate_id；指标数值由程序从证据填充并交叉校验。LLM 负责解释和建议，不重新计算实验成绩。无法观察的误认原因标注为待核验假设。
- 保存服务/模型/提示词版本、输入快照、原始响应与解析结果；失败显示真实错误，不产生假分析。无真实 LLM 时只能导出注明“未执行智能分析”的证据汇总。
- 综合 Markdown 报告、候选级 CSV、证据 JSON 绑定同一不可变快照；最终技术基线须人工确认，独立验证不足时保留待验证状态。

**我的测试与验证要求**：真实配置服务完成至少一次单 Run 和一次多 Run 分析；覆盖服务未配置、超时、鉴权失败、格式错误、引用不存在候选、编造指标、不可比 Run、超大上下文、证据版本变化。CSV/JSON/页面指标一致，旧报告内容不随新标注变化，导出不泄漏密钥。隔离协议测试通过不等于真实智能分析通过。

**需要你验收的点**：九项回答是否具体且有依据；推荐是否可追溯；证据不足时是否诚实；下载三种文件并核对一次配置及指标；确认是否接受候选技术基线。

**交付与通过门槛**：真实分析记录、报告样本、基线状态与校验结果。没有真实服务配置时，本阶段真实智能分析验收不得记为完成。

### 阶段 8：完整回归、正式迁移与最终验收

**开发目标**：在实际工作区稳定交付全链路，保留历史资料，并完成方案第 36 节验收流程。

**开发策略和规范**：

- 先在隔离副本完成全量回归、迁移和恢复演练，通过后备份正式工作区，再执行已验收迁移并做健康检查。
- 更新 README、A01 操作说明、迁移记录和独立的 v2 测试报告；保留旧测试报告历史，所有结果带日期、代码版本、环境及样本标识。
- 回滚明确代码与数据快照配套；若迁移后已产生新数据，先保全增量证据再恢复，不直接覆盖。
- 同时检查登录、文件上传/试听、引用删除保护、模型设置、数据仓和旧实验查看；不把旧实时能力的保留代码当作新 A01 功能。

**我的测试与验证要求**：全量后端、真实模型、TypeScript/生产构建、Playwright 全流程；桌面和窄屏检查；刷新/重启/取消/崩溃回归；连续至少 3 轮真实完整实验，任务结束后无孤儿 Worker、内存无持续异常增长；多次 Run 分析及三类下载一致；备份可恢复。

**需要你验收的点**：从选择建档录音开始，完成代表试听、目标确认、不同长度建档、多文件标注、自动比较、错误复核、保存 Run，再选择多次 Run、真实智能分析并下载报告。另抽查一个旧记录和一次失败场景。

**交付与通过门槛**：可运行代码、迁移与恢复工具、操作文档、v2 测试报告、证据索引、最终验收清单；全部必需验收项通过后才宣布 A01 改造完成。

## 五、全阶段质量规范

1. **真实结果红线**：正式产品不得返回模拟 Speaker、Embedding、相似度、指标、排名或 LLM 分析。测试用的固定数学输入、故障注入、协议替身只在隔离测试中使用，不入正式实验仓，不作为真实模型验收证据。
2. **测试环境隔离**：后端与浏览器测试使用独立 `MIAOWU_WORKSPACE` 和明确的测试服务配置；生产模型文件可只读复用，测试 Run、标注、报告不得混入正式工作区。
3. **证据完整性**：每条结果能追溯输入、参数、权重、标注、执行 Job 和原始产物。产物先完整写入再提交引用，防止进程崩溃留下看似成功的半成品。
4. **状态一致性**：前后端使用同一状态定义；运行期间不能任意改写冻结配置；失败、取消、等待人工、不可用及证据不足分别表达。
5. **计算规范**：拒绝 NaN/Inf，时间与时长统一单位，空数据与零分母显式处理；相似度与概率、距离阈值与相似度阈值不混淆。
6. **实现可维护性**：分离任务调度、模型适配、音频取样、评价、比较、报告职责；新增 API 使用明确 schema；业务关键逻辑不只在前端校验。
7. **性能可核验**：记录模型调用次数、复用情况、实际耗时和资源。阶段 0/1 获取本机基准后冻结合理超时及资源预算，不预先承诺未经测量的运行秒数。
8. **回归与修复**：围绕算法、状态、迁移、失败隔离和完整业务路径写有意义的测试；修复关键缺陷后补针对性回归。阻断流程、数据丢失、伪造结果、指标错误和崩溃隔离失败均阻止验收。
9. **通过与受阻分开**：测试通过、测试失败、因依赖跳过、待用户主观验收四类单列；缺少模型/授权/会议数据/LLM 服务时不得将对应验收项写为通过。
10. **每阶段可审阅**：维护阶段变更说明和验收证据；用户不需要从源码推断功能是否完成。重大范围或评价口径变更先更新计划，避免实施中悄悄改变结论含义。

## 六、验收数据和外部条件

阶段 0 盘点已有资料后复用，不要求重复提供已配置的服务或已有录音。最终验收建议具备：

- 一份真实多人建档会议，目标人物至少 60 秒可用非重叠语音，才可完整比较三档时长。
- 至少三份来自其它会议的测试录音，至少两份含目标、一份无目标；数量用于基本功能验收，不代表统计充分。
- 至少一份含目标的测试录音完成全部目标时间段标注；最终质量结论建议覆盖多个正负文件、噪声或跨设备场景。
- 如需批准正式技术基线，另有未参与调参的真实会议验证录音，并给出可接受的误认上限和最低召回要求。
- 至少两条真实可用 Discovery 路线和两个真实可用目标模型；pyannote 如缺授权仍显示不可用，不能冒充路线实测完成。
- 可真实调用的辅助分析助手服务；沿用设置中的配置，密钥仅通过设置管理。

这些条件缺失时先完成不受影响的开发与验证，同时记录具体受阻验收项；不能靠模拟结果补齐最终验收。

## 七、需求覆盖与阶段门禁摘要

| 方案要求 | 对应阶段 | 核心验收证据 |
|---|---|---|
| pyannote 与模型故障隔离（27～28） | 1、8 | 异常退出后主服务可用、错误与进程记录 |
| 两卡片、完整实验定义、退出实时（1～4、26、30～33） | 0、2、4、7、8 | 完整浏览器流程、Run/Job 关系、历史兼容 |
| 真实路线和必要参数（5～6） | 1、2、4 | 路线可用性实测、参数实际生效 |
| 代表试听、目标确认（7～8） | 2 | 试听产物、来源映射、多选及排除 |
| Profile 与模型筛选（9～10） | 4 | 时长核对、模型空间与能力校验 |
| 两级 Ground Truth（11） | 3、5 | 标注版本、评价资格和混淆矩阵 |
| 四变量、自动比较、阈值复用（12～14、17～19） | 4、6 | 候选数量、调用计数、排名与稳定区间 |
| 五项核心指标、DER/JER 辅助定位（15～16） | 5 | 固定公式、分母、真实数据核算；本期不伪造 DER/JER |
| 错误样本与相似度分布（20～21） | 5、6 | 错误试听、真实标签来源、未知样本处理 |
| 多 Run 分析、九项回答、技术结论与导出（22～26） | 7 | 真实 LLM、候选引用、三种导出一致 |
| 数据结构、真实运行、完整验收（29、34～38） | 0～8 | 迁移恢复、需求映射、端到端证据及用户验收 |

本计划批准后，先执行阶段 0，提交规则冻结与验证基线供验收，再按顺序开展各阶段。最终交付结论分别说明“系统功能是否完成”和“当前数据是否足以形成技术基线”；不能用前者代替后者。
