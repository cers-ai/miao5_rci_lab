# 妙悟·实时认知智能实验室 V0.1 开发方案

> 对应产品原型：`妙悟_实时认知智能实验室_V0.1.html`  
> V0.1 范围：**系统基础能力 + 模型设置 + A01 目标人物声纹建立与实时识别完整可用**。  
> 原则：先把 A01 做成真正能开展技术实验的产品，验收后再继续设计 A02 及后续能力。

---

# 1. V0.1 开发目标

V0.1 不是演示 Demo，也不是把 HTML 页面“照着画出来”。

本阶段要交付一个可真实使用的实验产品：

1. 用户可以登录系统；
2. 可以管理本地模型和模型服务；
3. 可以上传、保存、反复使用会议录音；
4. A01 的三个实验可以真实运行：
   - E1 说话人发现实验；
   - E2 目标人物识别实验；
   - E3 实时识别实验；
5. 每次实验的真实配置、真实输出、人工核验结果都能保存；
6. 第 04 步可以选择多轮实验结果，由配置好的“辅助分析智能助手”进行综合分析；
7. 可以形成并下载 A01 综合实验报告。

**V0.1 不开发 A02～B04 的实际功能，只保留导航占位。**

---

# 2. 技术架构

采用轻量的本地 Web 架构，不在 V0.1 引入 Kubernetes、Kafka、复杂微服务等基础设施。

```text
┌────────────────────────────────────────────┐
│              Web Frontend                  │
│ React + TypeScript + Vite                  │
│                                            │
│ 登录 / 实验页面 / 音频试听 / 实时状态 / 报告 │
└───────────────────┬────────────────────────┘
                    │ HTTP / WebSocket
                    ▼
┌────────────────────────────────────────────┐
│              Python Backend                │
│ FastAPI + asyncio                          │
│                                            │
│ Auth                                       │
│ File Service                               │
│ Model Registry                             │
│ Experiment Service                         │
│ Report / Analysis Service                  │
└───────────────┬───────────────┬────────────┘
                │               │
        ┌───────▼───────┐ ┌────▼────────────┐
        │ Local Models  │ │ Model Services  │
        │               │ │                 │
        │ pyannote      │ │ Provider / API  │
        │ FunASR/CAM++  │ │ Qwen /          │
        │ WeSpeaker     │ │ DeepSeek / ...  │
        └───────┬───────┘ └─────────────────┘
                │
        ┌───────▼────────────────────────────┐
        │ SQLite + Local File Workspace      │
        │                                    │
        │ 用户 / 模型配置 / 文件 / 实验记录   │
        │ 目标人物声纹 / 分析结果 / 报告       │
        └────────────────────────────────────┘
```

---

# 3. 推荐技术栈

## 3.1 前端

```text
React
TypeScript
Vite
Web Audio API
AudioWorklet
WebSocket
```

主要负责：

- 登录；
- 实验文件上传和试听；
- A01 实验配置；
- Speaker 结果展示和分段试听；
- 目标人物选择；
- 实时识别状态；
- 多轮实验结果比较；
- 综合分析和报告下载。

---

## 3.2 后端

```text
Python 3.11+
FastAPI
asyncio
Pydantic
SQLAlchemy
Alembic
SQLite
```

建议使用：

```text
uv + uv.lock
```

锁定 Python 依赖，保证开发机和验证机环境一致。

---

## 3.3 音频 / AI

```text
PyTorch
torchaudio
soundfile
ffmpeg
scikit-learn
FunASR
pyannote.audio
WeSpeaker
```

各自用途：

- `pyannote.audio`：A01-E1 说话人分离候选路线；
- `FunASR + CAM++`：A01-E1 另一条说话人发现候选路线；
- `WeSpeaker`：A01-E1 候选路线及 A01-E2 目标人物声纹识别；
- `scikit-learn`：CAM++ / WeSpeaker Embedding 后的聚类；
- `AudioWorklet + WebSocket`：A01-E3 实时音频进入 Python。

FunASR 官方工具链支持 VAD、流式 ASR、说话人等语音处理能力；WeSpeaker 面向 Speaker Verification / Recognition / Diarization；pyannote.audio 提供完整 Speaker Diarization Pipeline。开发时应固定实际使用的模型和依赖版本，不允许运行时自动漂移。

---

# 4. 项目目录建议

```text
miaowu-lab/
│
├── frontend/
│   └── src/
│       ├── pages/
│       │   ├── Home/
│       │   ├── A01/
│       │   ├── DataWarehouse/
│       │   └── Settings/
│       ├── components/
│       ├── audio/
│       └── api/
│
├── backend/
│   └── app/
│       ├── api/
│       ├── auth/
│       ├── files/
│       ├── models/
│       │   ├── registry/
│       │   ├── local/
│       │   └── services/
│       ├── experiments/
│       │   └── a01/
│       │       ├── speaker_discovery.py
│       │       ├── target_profile.py
│       │       ├── target_verification.py
│       │       └── realtime_lock.py
│       ├── analysis/
│       ├── reports/
│       └── db/
│
├── workspace/
│   ├── uploads/
│   ├── models/
│   ├── target_profiles/
│   ├── experiment_outputs/
│   └── reports/
│
├── scripts/
│   ├── init.py
│   ├── download_models.py
│   └── health_check.py
│
└── uv.lock
```

---

# 5. 初始化流程

必须提供一个真正可执行的初始化程序，例如：

```bash
python scripts/init.py
```

或者：

```bash
miaowu init
```

初始化完成以下动作。

## 5.1 创建运行环境

检查：

```text
Python
ffmpeg
CPU / CUDA GPU
磁盘空间
模型目录
数据目录
```

然后安装锁定依赖。

---

## 5.2 初始化数据库

第一次启动自动创建数据库和基础表。

V0.1 最少需要：

```text
users
local_models
model_services
experiment_files
target_profiles
experiment_runs
experiment_run_items
analysis_reports
```

---

## 5.3 创建默认用户

初始化：

```text
用户名：admin
密码：abcd@1234
角色：管理员
```

密码必须：

```text
Hash 后保存
```

数据库中禁止保存明文密码。

第一次正式部署后应允许管理员修改密码。

---

# 6. 初始化本地模型

V0.1 启动时，不应该让实验人员进入一个“空系统”。

初始化程序应下载 A01 必需的本地模型，并自动形成“系统设置 → 模型注册”中的初始记录。

建议初始化以下模型。

| 初始模型 | 能力 | 用于 |
|---|---|---|
| pyannote Speaker Diarization | 说话人发现 | A01-E1 |
| CAM++ Speaker Model | 说话人发现 / Speaker Embedding | A01-E1 |
| WeSpeaker 中文预训练 Speaker Model | 目标说话人识别 | A01-E1 / E2 |

说明：

### pyannote

如果所选 checkpoint 需要 Hugging Face Token 或模型协议确认：

- 初始化程序必须明确提示；
- 用户完成授权后再下载；
- 下载失败时模型状态显示为 `未安装`；
- 不能偷偷跳过后仍然显示“可用”。

### CAM++ / WeSpeaker

下载完成以后：

1. 保存到统一模型目录；
2. 写入模型注册表；
3. 启动时执行一次最小加载测试；
4. 测试成功后状态才显示为：

```text
可用
```

---

# 7. 本地模型注册

本地模型的定义：

> **由妙悟后台 Python 进程直接加载和运行的模型。**

V0.1 只需要三个能力类型：

```text
流式语音识别
说话人发现
目标说话人识别
```

目前 A01 实际使用后两类。

注册字段：

```text
模型名称
能力类型
加载方式
模型路径 / 模型标识
运行设备
备注
状态
```

加载方式 V0.1 只保留：

```text
Python 模块
命令行进程
```

每个本地模型必须支持：

```text
编辑
测试
启用 / 停用
```

其中：

**测试不能是假按钮。**

必须真正执行：

```text
加载模型
↓
执行一份内置小样本
↓
验证返回结果
↓
返回耗时 / 错误
```

---

# 8. 模型服务注册

模型服务定义：

> **通过 HTTP/API 调用的模型能力。**

不管服务物理部署在本机、局域网还是云端，只要通过 API 调用，统一按“模型服务”管理。

采用：

```text
Provider
    ↓
多个 Model
```

模型服务字段：

```text
服务名称
Provider
API Base URL
API Key
请求超时
可用模型名称
默认模型
备注
状态
```

例如：

```text
Provider：OpenAI Compatible
API URL：https://xxx/v1

Models：
- Qwen3.6-35B-A3B
- DeepSeek-V4-Flash
- MiniMax M2.7
```

支持：

```text
编辑
测试
启用 / 停用
```

“测试服务”必须真实执行：

```text
API连通
↓
模型列表/指定模型检查
↓
发送最小请求
↓
确认返回有效内容
```

API Key 不得明文显示在模型列表中。

---

# 9. 辅助分析智能助手

系统设置中配置：

```text
智能体名称
模型服务
模型名称
System Prompt
Temperature
Max Tokens
```

V0.1 不需要开发复杂 Agent Framework。

本质就是：

```text
Experiment Results
        ↓
Structured Context Builder
        ↓
Configured LLM
        ↓
Structured Analysis
```

建议返回结构化 JSON：

```json
{
  "summary": "",
  "findings": [],
  "recommended_configuration": {},
  "parameter_observations": [],
  "risks": [],
  "next_experiments": []
}
```

然后前端渲染成中文分析报告。

---

# 10. A01 必须完整开发

A01 不能只完成页面。

必须保证实验人员可以从头到尾真实完成一次实验。

---

# 11. A01-E1：说话人发现实验

目标：

> 对一段真实多人会议录音，比较不同 Speaker Discovery 方法，把多人真正分开。

## 11.1 输入

支持：

```text
上传新录音
选择历史录音
在线试听
```

上传后文件永久进入实验数据仓。

保存：

```text
原始文件
文件名称
时长
采样率
声道
上传时间
文件Hash
```

---

## 11.2 实验路线

V0.1 实际打通：

### Route 1

```text
FunASR VAD
→ CAM++ Embedding
→ Clustering
```

### Route 2

```text
pyannote Speaker Diarization
```

### Route 3

```text
WeSpeaker Embedding
→ Clustering
```

如果某条路线暂时无法真正打通：

> 宁可在 UI 中显示“当前不可用”，也禁止返回模拟结果。

---

## 11.3 参数

只显示该路线真正使用的参数。

例如：

```text
Speaker Count
Clustering Threshold
Minimum Speech Duration
```

不要为了“看起来专业”堆无实际作用的参数。

---

## 11.4 真实执行结果

后台必须返回：

```json
{
  "speakers": [
    {
      "speaker_id": "SPEAKER_01",
      "total_duration_ms": 0,
      "segments": [
        {
          "start_ms": 0,
          "end_ms": 0,
          "confidence": 0.0
        }
      ]
    }
  ],
  "elapsed_ms": 0
}
```

所有数据由真实模型产生。

---

## 11.5 人工试听核验

实验人员必须可以：

```text
试听 Speaker 01 代表片段
试听 Speaker 02 代表片段
...
```

并记录：

```text
正确
同一人被拆成多个Speaker
不同人被错误合并
其它问题
```

人工核验结果保存进当前 Experiment Run。

---

## 11.6 多轮实验

调整方法或参数再次运行：

```text
Run 001
Run 002
Run 003
...
```

每次 Run 独立保存：

```text
输入文件
技术路线
模型版本
参数
原始输出
人工核验
运行耗时
创建时间
```

禁止覆盖上一轮实验。

---

# 12. A01-E2：目标人物识别实验

目标：

> 从 E1 结果中选中某个人，建立目标人物声纹，然后验证其它录音里是否能认出他。

---

## 12.1 建立目标人物声纹

实验人员：

```text
试听 E1 Speaker
↓
勾选目标人物对应的 Speaker
↓
必要时合并多个 Speaker
↓
排除明显错误 / 重叠片段
↓
保存目标人物
```

后台：

1. 提取约 30～60 秒高质量目标人物语音；
2. 使用当前 Speaker Verification Model 生成 Embedding；
3. 保存为 Target Profile。

保存：

```text
target_profile_id
人物名称
来源录音
来源segment
speaker model
embedding
创建时间
```

---

## 12.2 识别实验

选择：

```text
多个历史/新会议录音
Speaker Verification Model
Similarity Threshold
```

后台真实运行：

```text
Audio
↓
Speech Segments
↓
Speaker Embedding
↓
与 Target Profile 比较
↓
Similarity
↓
Target / Non-target
```

---

## 12.3 结果

每份录音显示：

```text
是否识别到目标人物
最高相似度
目标人物出现时间段
目标片段试听
```

人工核验：

```text
正确
误识别
漏识别
```

并保存为实验记录。

---

# 13. A01-E3：实时识别实验

目标：

> 已经知道“这个人是谁”以后，验证在实时会议中多久能锁定他，以及是否稳定。

---

## 13.1 输入方式

必须支持两种：

### 实时麦克风

```text
Browser
→ AudioWorklet
→ PCM
→ WebSocket
→ Python
```

这是验证真实实时运行。

### 历史录音实时回放

历史录音按照真实时间速度送入同一 Runtime。

用途：

> 做可重复的实时性能实验。

**正式性能指标建议主要基于“固定录音实时回放”，因为可重复。**

---

## 13.2 Runtime

```text
Audio Stream
↓
VAD
↓
Sliding Speaker Window
↓
Speaker Embedding
↓
Target Similarity
↓
Target Lock State Machine
```

状态至少包括：

```text
NON_TARGET
CANDIDATE
LOCKED
UNLOCKED
```

---

## 13.3 参数

只保留：

```text
Lock Threshold
Unlock Threshold
Consecutive Confirm Windows
```

---

## 13.4 实时页面

必须展示真实：

```text
当前相似度
当前状态
Speaker Timeline
Lock / Unlock Event
```

不能播放预设动画。

---

## 13.5 指标

至少计算：

```text
首次锁定时延
误锁次数
解锁时延
重新锁定时延
目标人物覆盖率
```

如果是固定录音：

通过人工 Ground Truth 计算正式指标。

如果是现场麦克风：

主要用于运行观察，并允许人工标记事件。

---

# 14. A01 第04步：综合结论与报告

显示：

```text
E1 多轮实验
E2 多轮实验
E3 多轮实验
```

允许实验人员：

```text
勾选若干有代表性的 Run
```

然后：

```text
综合分析
```

后台构造：

```json
{
  "scene": "A01",
  "selected_runs": [],
  "manual_reviews": [],
  "metrics": [],
  "model_versions": [],
  "parameters": []
}
```

发送给：

```text
辅助分析智能助手
```

智能助手输出：

1. E1 推荐技术路线；
2. E2 推荐目标人物识别模型和阈值；
3. E3 推荐实时 Lock / Unlock 参数；
4. 参数敏感性；
5. 已发现的问题；
6. 当前适用边界；
7. 下一轮建议实验。

分析结果必须保存。

---

# 15. 综合报告

最终支持下载：

```text
A01_综合实验报告.md
A01_所选实验数据.csv
A01_实验证据.json
```

Markdown 报告至少包含：

```text
实验目的
实验环境
本地模型版本
选择的实验Run
E1结果比较
E2结果比较
E3结果比较
人工核验
智能综合分析
推荐技术配置
适用边界
已知风险
```

---

# 16. 实验数据仓

V0.1 保持简单，只需要三个 Tab。

## 实验文件

```text
上传文件
试听
查看基本信息
删除
```

## 实验记录

查看所有：

```text
E1 / E2 / E3 Run
```

支持打开查看完整实验详情。

## 实验报告

查看和重新下载已经生成的综合报告。

---

# 17. 严禁 Mock 数据

这一条必须作为 V0.1 的开发红线。

原型 HTML 中存在：

```text
4人
37段
0.87
470ms
```

这些都是为了表达页面效果的示例值。

**正式产品开发时必须全部删除。**

禁止：

```javascript
result = {
  speakerCount: 4,
  similarity: 0.87
}
```

禁止：

```text
点击“开始实验”
→ sleep 2秒
→ 展示预设结果
```

禁止：

```text
点击“测试模型”
→ 无条件显示“测试成功”
```

---

# 18. 所有结果必须来自真实链路

例如 E1：

```text
真实音频
→ 真实模型
→ 真实 Speaker Segment
→ UI
```

E2：

```text
真实 Target Embedding
+
真实测试录音
→ 真实 Similarity
→ UI
```

E3：

```text
真实 Audio Stream
→ 真实 Sliding Verification
→ 真实 Lock Event
→ 真实 Latency
```

综合分析：

```text
真实 Experiment Run
→ 真实模型服务
→ 真实 LLM Response
```

---

# 19. 失败时必须显示失败

如果出现：

```text
模型没下载
CUDA不可用
模型加载失败
音频格式错误
API Key错误
API服务超时
```

系统必须：

```text
FAIL
+
明确错误原因
```

而不是用 Mock 结果把流程继续跑下去。

---

# 20. 数据真实性要求

每一个实验结果必须可以追溯到：

```text
哪个文件
哪个模型
哪个模型版本
哪些参数
什么时候运行
模型原始输出是什么
人工做了什么核验
最终指标是什么
```

这是妙悟作为“实验室”最基本的可信性要求。

---

# 21. 推荐后端 API

保持最小化。

## Auth

```text
POST /api/login
POST /api/logout
GET  /api/me
```

## 文件

```text
POST   /api/files
GET    /api/files
GET    /api/files/{id}
DELETE /api/files/{id}
GET    /api/files/{id}/audio
```

## 本地模型

```text
GET  /api/models/local
POST /api/models/local
PUT  /api/models/local/{id}
POST /api/models/local/{id}/test
```

## 模型服务

```text
GET  /api/model-services
POST /api/model-services
PUT  /api/model-services/{id}
POST /api/model-services/{id}/test
```

## A01

```text
POST /api/a01/e1/run
POST /api/a01/target-profile
POST /api/a01/e2/run

WS   /api/a01/e3/realtime
POST /api/a01/e3/replay

GET  /api/a01/runs
GET  /api/a01/runs/{id}
```

## 综合分析

```text
POST /api/a01/analyze
POST /api/a01/report
```

---

# 22. 开发顺序

建议严格按照以下顺序。

## Step 1：项目骨架

完成：

```text
React
FastAPI
SQLite
登录
文件上传
文件试听
系统设置骨架
```

---

## Step 2：模型初始化与注册

完成：

```text
init脚本
本地模型下载
本地模型真实加载测试
模型服务真实API测试
```

验收后再进入 A01。

---

## Step 3：A01-E1

打通：

```text
真实录音
→ Speaker Discovery
→ Speaker Segment
→ 试听
→ 人工核验
→ 多轮Run
```

**E1 独立验收。**

---

## Step 4：A01-E2

打通：

```text
选择Speaker
→ Target Profile
→ 新录音验证
→ Similarity
→ 目标片段试听
→ 人工核验
```

**E2 独立验收。**

---

## Step 5：A01-E3

打通：

```text
AudioWorklet / 实时回放
→ WebSocket
→ Speaker Verification
→ Lock State Machine
→ 实时指标
```

**E3 独立验收。**

---

## Step 6：第04步综合分析

打通：

```text
选择真实Run
→ 调用真实模型服务
→ 综合分析
→ 保存结果
→ 导出报告
```

---

# 23. V0.1 最终验收标准

开发团队交付后，验证人员必须能够独立完成下面这条真实流程：

```text
登录
↓
确认本地模型可用
↓
上传一段多人会议录音
↓
E1真实运行3种不同配置
↓
逐个试听Speaker结果
↓
选择目标人物
↓
建立真实Target Profile
↓
上传/选择多份其它会议录音
↓
E2真实验证目标人物
↓
选择推荐识别配置
↓
E3进行真实实时识别
↓
产生多轮真实实验记录
↓
在第04步勾选若干Run
↓
调用真实大模型进行综合分析
↓
下载A01综合实验报告
```

如果这一流程中任何关键结果仍来自前端硬编码、随机数或 Mock API：

> **V0.1 不通过验收。**

---

# 24. V0.1 的完成定义

V0.1 完成并不意味着“妙悟实验室开发完成”。

它只意味着第一项能力真正打穿：

> **妙悟已经从一个 HTML 需求原型，变成了一套可以真实开展“目标人物声纹建立与实时识别”技术实验的产品。**

A01 验收完成以后，再在同一产品骨架上继续设计和开发：

```text
A02 流式语音实时识别
A03 复杂声音场景多目标人物混合识别
A05 实时图像 + 音频多模态实验
B01 控制环路实验
...
```

坚持：

> **设计一个 → 开发一个 → 真实实验 → 验收一个 → 再进入下一个。**
