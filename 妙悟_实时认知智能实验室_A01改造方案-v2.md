# 妙悟·实时认知智能实验室 A01 改造方案 v2

> 适用版本：`妙悟_实时认知智能实验室_V0.1`  
> 改造范围：仅 A01  
> 核心范式：**一个实验室卡片 + 一个分析与报告卡片**  
> A01 唯一目标：**找到目标人物声纹档案建立与离线识别的最佳技术路径和参数组合。**

---

# 1. A01 最终结构

A01 只保留两个一级卡片：

```text
01 目标人物声纹识别实验室
02 分析与报告
```

不再拆成：

```text
说话人发现
目标人物识别
实时识别
```

原因很简单：

> “说话人发现”只是目标人物声纹识别链路中的中间能力，不应该成为独立实验目标。

A01 最终只回答一个问题：

> **用什么 Speaker Discovery 路线、什么目标说话人模型、多少建档语音、什么识别阈值，能够在其它会议录音中最准确地识别目标人物？**

实时识别、Lock/Unlock、实时延迟等能力移到后续独立实验，不放在 A01。

---

# 2. 后续统一实验范式

以后 A02、A03、B01 等实验统一采用：

```text
实验室
+
分析与报告
```

## 实验室卡片

负责：

```text
选择输入
配置技术路线
设置必要参数
开始实验
真实执行
保存 Experiment Run
```

## 分析与报告卡片

负责：

```text
查看一次或多次 Run
选择需要比较的 Run
调用辅助分析智能助手
形成明确结论
下载综合报告
```

原则：

> **每一个实验只验证一个聚焦目标，并且一次或多次实验以后必须能够形成明确、可执行的技术结论。**

---

# 3. 卡片一：目标人物声纹识别实验室

目标：

> **找到目标人物声纹建档与离线识别的最佳方案。**

一轮实验完整链路：

```text
建档录音
→ Speaker Discovery
→ 确认目标人物
→ 建立 Target Profile
→ 测试录音识别
→ 自动计算指标
→ 保存本次 Experiment Run
```

这一整条链路才叫一次实验。

---

# 4. 实验输入

页面只需要两类录音。

## 4.1 建档录音

选择一份多人会议录音，例如：

```text
Meeting-01.wav
```

用途：

> 从这份录音中发现 Speaker，并确认目标人物，建立声纹档案。

支持：

```text
选择历史录音
上传新录音
试听
```

---

## 4.2 测试录音

选择多份其它会议录音，例如：

```text
Meeting-02.wav
Meeting-03.wav
Meeting-04.wav
Meeting-05.wav
```

用途：

> 验证建立出来的 Target Profile 能否在其它会议中正确识别目标人物。

同样支持：

```text
选择历史录音
上传新录音
试听
```

---

# 5. Speaker Discovery

实验人员选择 Speaker Discovery 路线。

V0.1 只保留真正开发完成的路线，例如：

```text
pyannote Speaker Diarization

FunASR VAD
+ CAM++ Embedding
+ Clustering

WeSpeaker Embedding
+ Clustering
```

没有开发完成的路线：

```text
显示“不可用”
```

不能返回模拟结果。

---

# 6. 参数保持简单

只显示当前路线真正使用、且实验人员能够理解的参数。

例如：

```text
预期说话人数
最短有效语音
聚类阈值
```

其中：

```text
聚类阈值
```

只对聚类路线显示。

不要为了“看起来专业”暴露大量底层算法参数。

---

# 7. Speaker 试听必须改造

当前实际使用中最大的痛点之一是：

> Diarization 把录音切成大量短片段，人耳很难判断哪个 Speaker 对应哪个人。

因此后台仍保存所有原始 Speaker Segment，但 UI 不再要求实验人员逐段试听。

系统自动为每个 Speaker 生成：

# Speaker 代表试听音频

建议规则：

```text
1. 排除过短片段
2. 排除明显重叠讲话
3. 优先选择较长、较清晰片段
4. 选择 3～5 个代表片段
5. 拼接成约 15～30 秒音频
6. 片段之间插入短暂静音
```

页面显示：

| Speaker | 总语音时长 | 片段数 | 操作 |
|---|---:|---:|---|
| Speaker 01 | 12m34s | 14 | 试听 |
| Speaker 02 | 8m17s | 11 | 试听 |
| Speaker 03 | 5m43s | 8 | 试听 |

实验人员通过一次试听即可判断：

```text
Speaker 02 = Leader-A
```

---

# 8. 确认目标人物

实验人员试听后：

```text
选择一个或多个 Speaker
→ 指定为目标人物
```

如果同一个人被算法拆成：

```text
Speaker 02
Speaker 05
```

则允许多选并合并。

同时允许排除：

```text
错误片段
重叠讲话
明显低质量片段
```

然后点击：

```text
建立目标人物声纹档案
```

---

# 9. Target Profile

后台根据人工确认的目标人物语音生成 Speaker Embedding。

V0.1 建议将建档时长作为实验变量：

```text
15秒
30秒
60秒
```

实验人员不需要手工剪音频。

系统自动从目标人物的高质量 Segment 中选择对应时长。

Target Profile 至少保存：

```text
profile_id
person_name
source_audio_id
source_speaker_ids
used_segments
speaker_model
profile_duration
embedding
created_at
```

---

# 10. 目标说话人识别模型

实验人员选择：

```text
目标说话人识别模型
```

页面只显示：

```text
系统设置
→ 本地模型
→ 能力类型 = 目标说话人识别
→ 状态 = 可用
```

的模型。

例如：

```text
WeSpeaker ECAPA
CAM++
```

不显示任何大语言模型、多模态模型等无关模型。

---

# 11. Ground Truth

为了让实验能自动判断“好坏”，必须提供最小 Ground Truth。

V0.1 分两级。

## Level 1：文件级 Ground Truth

每个测试文件只标记：

```text
目标人物是否出现
```

例如：

| 文件 | Leader-A |
|---|---|
| Meeting-02 | 有 |
| Meeting-03 | 有 |
| Meeting-04 | 无 |

这一层必须实现。

---

## Level 2：目标人物时间段 Ground Truth

对少量代表性测试录音，标记目标人物出现时间段：

```text
03:20 - 03:47
08:12 - 08:56
13:07 - 13:29
```

不需要标记其它 Speaker。

这一层用于自动计算正式的目标人物识别指标。

---

# 12. 一次实验允许比较的变量

A01 不开发通用 Grid Search。

只比较 4 类变量：

```text
1. Speaker Discovery Route
2. Target Speaker Model
3. Profile Duration
4. Similarity Threshold
```

实验人员可以：

```text
运行单个组合
```

也可以：

```text
运行自动对比
```

---

# 13. 自动对比

例如选择：

```text
Discovery：
pyannote
CAM++ + Clustering

Target Model：
WeSpeaker
CAM++

Profile：
15s
30s
60s
```

然后系统自动执行组合。

Similarity Threshold：

```text
0.60 ～ 0.90
```

可自动扫描。

---

# 14. Threshold 扫描不要重复执行模型

实现必须优化为：

```text
Audio
→ Embedding
→ Similarity
```

只计算一次。

然后针对：

```text
Threshold = 0.60
0.62
0.64
...
0.90
```

重新计算判断和指标即可。

禁止每调整一个 Threshold 就重新运行 Speaker Model。

---

# 15. 自动评价指标

A01 核心指标固定为：

```text
Target Precision
Target Recall
Target F1
False Accept
Miss Rate
```

其中必须重点展示：

# False Accept

即：

> 把其它人错误识别成目标人物。

这是实时认知系统的重要风险，因为错误人物数据可能继续污染认知、记忆和经验。

---

# 16. Speaker Discovery 指标定位

DER / JER 不是 A01 的核心目标。

如果未来有完整多人 Ground Truth：

```text
DER
JER
```

可以作为辅助指标。

A01 最终仍然以：

```text
目标人物识别效果
```

评价整条 Pipeline。

---

# 17. 一次 Experiment Run

一次完整实验结束后形成一条 Run。

例如：

```text
Run ID：A01-0008

建档录音：
Meeting-01.wav

测试录音：
Meeting-02 / 03 / 04 / 05

Discovery：
pyannote

Target Model：
WeSpeaker ECAPA

Profile：
30s

Threshold：
0.78

Precision：
98.4%

Recall：
94.7%

F1：
96.5%

False Accept：
1.2%

Miss：
5.3%
```

所有数据必须来自真实运行。

---

# 18. 自动排名

如果一次 Run 中比较多个组合，则展示排名：

| 排名 | Discovery | Target Model | Profile | Threshold | Precision | Recall | F1 | False Accept |
|---:|---|---|---:|---:|---:|---:|---:|---:|
| 1 | pyannote | WeSpeaker | 30s | 0.78 | ... | ... | ... | ... |
| 2 | pyannote | WeSpeaker | 60s | 0.78 | ... | ... | ... | ... |
| 3 | CAM++聚类 | WeSpeaker | 30s | 0.78 | ... | ... | ... | ... |

支持按：

```text
F1
Precision
Recall
False Accept
```

排序。

---

# 19. 稳定参数区间

系统不只显示：

```text
最佳 Threshold = 0.78
```

还要比较邻近值：

```text
0.76
0.78
0.80
```

如果性能接近：

```text
推荐：0.78
稳定区间：0.76 ～ 0.80
```

如果轻微变化就明显恶化：

```text
提示：当前参数较敏感
```

V0.1 只做简单邻近参数比较，不需要复杂统计模型。

---

# 20. 错误样本

实验结束后自动整理：

```text
False Accept
Miss
临界样本
```

例如：

| 类型 | 文件 | 时间段 | Similarity | Threshold | 操作 |
|---|---|---|---:|---:|---|
| False Accept | M03 | 06:23-06:31 | 0.81 | 0.78 | 试听 |
| Miss | M02 | 11:08-11:22 | 0.76 | 0.78 | 试听 |
| 临界样本 | M05 | 03:11-03:18 | 0.779 | 0.78 | 试听 |

以后人工主要听：

> **错误样本，而不是全部 Speaker 碎片。**

---

# 21. 相似度分布

建议增加简单分析图：

```text
Target Scores
Non-target Scores
```

目的：

> 帮助实验人员理解 Threshold 为什么应该设在某个位置。

如果两组明显分开：

```text
Non-target            Target
██████████        ███████████
          ↑
       Threshold
```

说明当前：

```text
Target Profile + Target Model
```

质量较好。

如果两组严重重叠：

> 问题不是简单调 Threshold，而可能需要更好的建档语音、识别模型或 Discovery Route。

---

# 22. 卡片二：分析与报告

第二个一级卡片只负责：

> **对一次或多次 Experiment Run 做综合分析并形成明确结论。**

页面显示历史 Run：

| 选择 | Run | 时间 | 最佳组合 | F1 | False Accept |
|---|---|---|---|---:|---:|
| ✓ | A01-0005 | ... | pyannote + WeSpeaker + 30s | ... | ... |
| ✓ | A01-0008 | ... | pyannote + WeSpeaker + 60s | ... | ... |
|  | A01-0010 | ... | CAM++ + WeSpeaker + 30s | ... | ... |

实验人员可以选择：

```text
一次 Run
或
多次 Run
```

然后点击：

# 智能分析

---

# 23. 辅助分析智能助手

调用：

```text
系统设置
→ 智能体设置
→ 辅助分析智能助手
```

中配置的模型服务与 Model。

发送数据建议结构：

```json
{
  "scene": "A01",
  "goal": "目标人物声纹建档与离线识别最优方案",
  "selected_runs": [],
  "metrics": [],
  "error_cases": [],
  "parameter_stability": [],
  "manual_reviews": []
}
```

---

# 24. 智能分析必须回答

至少输出：

```text
1. 哪条 Speaker Discovery Route 更好
2. 哪种 Target Speaker Model 更好
3. 建档 15s / 30s / 60s 哪个更合理
4. 推荐 Similarity Threshold
5. Threshold 稳定区间
6. False Accept 主要来自哪些情况
7. 当前方案失败边界
8. 是否已经可以形成技术基线
9. 下一次实验还需要补什么
```

---

# 25. 最终技术结论

最终分析结果必须足够明确，例如：

```text
推荐 Discovery：
xxx

推荐 Target Model：
xxx

推荐 Profile：
xx 秒

推荐 Similarity Threshold：
xxx

稳定区间：
xxx ～ xxx

Precision：
xxx

Recall：
xxx

F1：
xxx

False Accept：
xxx

主要错误：
xxx

适用边界：
xxx

下一步：
xxx
```

具体值必须来自真实实验。

---

# 26. 报告下载

只在“分析与报告”卡片提供：

```text
下载综合实验报告
下载实验数据 CSV
下载实验证据 JSON
```

实验室卡片不单独提供报告下载。

结构固定为：

```text
实验室
→ 产生 Run

分析与报告
→ 消费 Run
→ 智能分析
→ 明确结论
→ 下载报告
```

---

# 27. pyannote 稳定性改造

当前 pyannote 已出现：

> 一运行导致 FastAPI 主服务挂掉。

不管最终原因是 OOM、CUDA、PyTorch、pyannote Bug 还是开发代码问题，产品架构上都必须隔离模型执行。

建议：

```text
FastAPI 主服务
      │
      │ 创建实验 Job
      ▼
独立 Model Worker
      │
      ├── pyannote
      ├── CAM++
      └── WeSpeaker
```

V0.1 可以使用：

```text
multiprocessing
或 subprocess
```

不需要引入 Celery / Kafka。

---

# 28. Worker 最小要求

记录：

```text
job_id
model
pid
started_at
finished_at
status
exit_code
error
peak_memory
```

如果模型崩溃：

```text
Experiment Run = FAILED
```

FastAPI 主服务必须继续运行。

前端显示真实错误。

---

# 29. 数据结构最小改造

## target_profiles

```text
id
person_name
source_audio_id
speaker_model
profile_duration
embedding_path
created_at
```

## ground_truth_files

```text
id
audio_id
target_person
target_present
```

## ground_truth_segments

```text
id
audio_id
target_person
start_ms
end_ms
```

## experiment_runs

```text
id
scene
status
input_config
started_at
finished_at
```

## experiment_candidates

一条 Run 中允许多个候选组合：

```text
run_id
discovery_route
target_model
profile_duration
threshold
precision
recall
f1
false_accept
miss_rate
```

## recognition_segments

```text
candidate_id
audio_id
start_ms
end_ms
similarity
predicted_target
ground_truth_target
```

---

# 30. A01 页面改造

原：

```text
01 说话人发现
02 目标人物识别
03 实时识别
04 综合结论
```

改为：

```text
01 目标人物声纹识别实验室
02 分析与报告
```

---

# 31. “实验室”卡片页面顺序

建议：

```text
1. 实验目标

2. 建档录音

3. Speaker Discovery
   - 路线
   - 必要参数

4. Speaker结果
   - Speaker列表
   - 代表试听
   - 目标人物确认

5. Target Profile
   - Target Model
   - Profile Duration

6. 测试录音
   - 文件选择
   - Target有/无
   - 可选Target时间段

7. 实验方案
   - 单组合实验
   - 自动对比实验

8. 开始实验

9. 实验结果
   - 指标
   - 组合排名
   - 稳定区间
   - 错误样本
   - 相似度分布

10. 保存本次 Experiment Run
```

---

# 32. “分析与报告”卡片页面顺序

```text
1. 历史实验结果

2. 勾选一次或多次 Run

3. 查看所选结果摘要

4. 智能分析

5. 智能分析结果

6. 最终技术基线

7. 下载综合报告
```

---

# 33. 统一实验定义

以后妙悟中的每个 Experiment Run 都必须满足：

```text
一个明确实验目标
+
一组明确输入
+
一组技术配置
+
真实执行
+
明确指标
+
保存结果
```

不能把：

```text
上传
试听
建档
分析
```

分别包装成独立实验。

这些只是实验内部步骤。

---

# 34. 开发红线

继续执行：

# 禁止 Mock 数据

以下必须全部来自真实计算：

```text
Speaker Segments
Speaker试听样本
Target Embedding
Similarity
Precision
Recall
F1
False Accept
Miss Rate
参数比较
模型测试
智能分析
```

失败时必须：

```text
明确显示 FAILED
+
真实错误原因
```

不能返回假结果。

---

# 35. 本次改造开发顺序

## Step 1
修复 pyannote Worker 隔离。

## Step 2
实现 Speaker 代表试听音频。

## Step 3
将原“说话人发现 + 目标人物识别”合并成一个实验室页面。

## Step 4
实现文件级 Target 有/无 Ground Truth。

## Step 5
实现少量 Target 时间段标注。

## Step 6
实现 Target Profile 和跨录音识别。

## Step 7
实现 Precision / Recall / F1 / False Accept / Miss。

## Step 8
实现 A01 专用自动组合比较。

## Step 9
实现错误样本和相似度分布。

## Step 10
实现“分析与报告”卡片和真实 LLM 智能分析。

---

# 36. 改造验收标准

验证人员必须能够完成：

```text
选择建档录音
↓
运行 Speaker Discovery
↓
通过代表试听确认目标人物
↓
选择 Target Model
↓
建立不同长度 Target Profile
↓
选择多份测试录音
↓
设置最小 Ground Truth
↓
开始一次真实实验
↓
自动得到多个候选组合结果
↓
查看 Precision / Recall / F1 / False Accept
↓
查看错误样本
↓
保存 Run
↓
再运行若干次实验
↓
进入“分析与报告”
↓
选择一次或多次 Run
↓
调用真实辅助分析智能助手
↓
得到明确推荐路线和参数
↓
下载综合报告
```

---

# 37. A01 最终必须回答的问题

改造完成以后，A01 只回答一个问题：

> **在当前真实实验数据条件下，目标人物声纹档案应该采用什么 Speaker Discovery 路线、什么目标说话人模型、多少建档语音以及什么 Similarity Threshold，才能取得更高的识别准确性和更低的误认风险？**

如果系统能够稳定回答这个问题：

> **A01 实验完成。**

实时锁定、实时延迟、Lock / Unlock 等问题：

> **移到后续独立实验。**

---

# 38. 妙悟后续统一设计原则

所有后续实验统一采用：

```text
┌──────────────────────┐
│       实验室          │
│                      │
│ 输入                  │
│ 技术配置              │
│ 开始实验              │
│ 真实指标              │
│ Experiment Run        │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────┐
│     分析与报告        │
│                      │
│ 选择一次/多次 Run     │
│ 智能分析              │
│ 明确技术结论          │
│ 综合报告              │
└──────────────────────┘
```

核心要求：

> **每个实验必须足够聚焦，使一次或多次实验之后能够形成明确、可执行的技术结论。**
